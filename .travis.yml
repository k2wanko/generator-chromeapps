language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chromeapps' ]; then cd .. && eval "mv $currentfolder generator-chromeapps" && cd generator-chromeapps; fi
